/* Backend configuration.
   Leave both empty → the app runs in demo mode (data stays on the device).
   Fill them with your Supabase project's URL and anon key → real accounts, real electricians, live updates.
   The anon key is safe to publish: access is controlled by the database policies in supabase/schema.sql. */
window.LOGON_CONFIG = {
  supabaseUrl: '',
  supabaseAnonKey: ''
};
